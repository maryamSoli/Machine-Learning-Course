# Movie-Recommendation-System-ML-Final-Project
Step 1: Create a Virtual Environment:# For Python 3
python -m venv venv

Step 2: Activate the Virtual Environment: venv\Scripts\activate

Step 3:pip install -r requirements.txt

Step 4:run app.py using python app.py command
